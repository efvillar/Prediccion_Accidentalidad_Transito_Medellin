---
title: "MODELO FRECUENCIA MENSUAL"
author: "Edwin Villarraga-Jorge Montoya-Juan David Arango"
date: "10/8/2019"
output:
    html_document:
      theme: yeti
    toc: yes
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(sqldf)
```


# EVALUACION DE MODELOS

## 1. Importación y estandarización de la información

```{r}
load("../data/Frecuencia_De_Accidentes_Mensual.Rda")
```

```{r}
load("../data/Dias_Especiales_Mensuales.Rda")
```

Se crean las columnas de accidentes Graves y leves para saber la frecuencia por semana

```{r}
library(reshape)
Total_Dataset_Freq_M <- cast(Total_Dataset_Freq_M[,c(1,2,3,4)],ANO+MES~GRAVEDAD)
```
Se agrega la columna TOTAL_ACCIDENTES

```{r}
Total_Dataset_Freq_M$TOTAL_ACCIDENTES <- Total_Dataset_Freq_M$ACCIDENTES_GRAVES + Total_Dataset_Freq_M$ACCIDENTES_LEVES
```

```{r}
Total_Dataset_Freq_M <- sqldf("SELECT* 
              FROM Total_Dataset_Freq_M FS
              LEFT JOIN Dias_Especiales_Mensuales DES 
              ON (FS.ANO=DES.ANO AND FS.MES=DES.MES)")
```

```{r}
Total_Dataset_Freq_M[6]<- NULL
Total_Dataset_Freq_M[6]<- NULL
```


## 2. Particion de los datos para entrenamiento y test

Se ajustarán modelos con la información disponible desde el 01 de enero de 2014 hasta el 31 de diciembre de 2017 y se utilizará el año 2018 para validar el modelo:

```{r}
Train_M_Dataset <- subset(Total_Dataset_Freq_M, ANO!="2018")
```

```{r}
summary(Train_M_Dataset$ANO)
```

Se ajustan otra vez los niveles del factor ANO

```{r}
Train_M_Dataset$ANO <- factor(Train_M_Dataset$ANO)
```

```{r}
summary(Train_M_Dataset$ANO)
```




```{r}
library(sqldf)
Test_M_Dataset <- sqldf("SELECT *  
       FROM Total_Dataset_Freq_M
       WHERE ANO == 2018")
```

```{r}
summary(Test_M_Dataset$ANO)
```

Se ajustan otra vez los niveles del factor ANO

```{r}
Test_M_Dataset$ANO <- factor(Test_M_Dataset$ANO)
```

```{r}
summary(Test_M_Dataset$ANO)
```

## 3. Selección de las mejores variables para el modelo 

Se utilizará el método best subset selection para elegir las mejores variables explicativas del modelo teniendo como criterio aquellas variables que presente R ajustado 

#### Selección de variables para Total accidentes
```{r}
library (leaps)
regfit.full=regsubsets (TOTAL_ACCIDENTES∼Freq_lunes+Freq_martes+Freq_miercoles+Freq_jueves+Freq_viernes+Freq_sabado+Freq_domingo+Feriados+Feriados_Lunes+Feriados_Otro,Train_M_Dataset, nvmax =8)
summary (regfit.full)
```

```{r}
reg.summary =summary(regfit.full)
names(reg.summary)
```
```{r}
reg.summary$rsq
```



```{r}
which.max (reg.summary$adjr2)
```


```{r}
par(mfrow =c(2,2))
plot(reg.summary$rss ,xlab=" Número de Variables ",ylab=" RSS",
type="l")
plot(reg.summary$adjr2 ,xlab =" Número de Variables ",
ylab=" Rsq Ajustado",type="l")

points (3, reg.summary$adjr2[3], col ="red",cex =2, pch =20)
```
```{r}
which.min (reg.summary$cp)
which.min (reg.summary$bic)
```



```{r}
plot(reg.summary$cp ,xlab =" Número de Variables ",ylab="Cp",
type='l')
points (3, reg.summary$cp [3], col ="red",cex =2, pch =20)

plot(reg.summary$bic ,xlab=" Número de Variables ",ylab=" BIC",
type='l')
points (3, reg.summary$bic [3], col =" red",cex =2, pch =20)
```
```{r}
plot(regfit.full ,scale ="r2")
plot(regfit.full ,scale ="adjr2")
plot(regfit.full ,scale ="Cp")
plot(regfit.full ,scale ="bic")
```
```{r}
coef(regfit.full ,3)
```


#### Selección de variables para accidentes graves
```{r}
library (leaps)
regfit.full=regsubsets (ACCIDENTES_GRAVES∼Freq_lunes+Freq_martes+Freq_miercoles+Freq_jueves+Freq_viernes+Freq_sabado+Freq_domingo+Feriados+Feriados_Lunes+Feriados_Otro,Train_M_Dataset, nvmax =19)
summary (regfit.full)
```

```{r}
reg.summary =summary(regfit.full)
names(reg.summary)
```


```{r}
reg.summary$rsq
```

```{r}
par(mfrow =c(2,2))
plot(reg.summary$rss ,xlab=" Número de Variables ",ylab=" RSS",
type="l")
plot(reg.summary$adjr2 ,xlab =" Número de Variables ",
ylab=" RSq Ajustado",type="l")
which.max (reg.summary$adjr2)
points (3, reg.summary$adjr2[3], col ="red",cex =2, pch =20)
```

```{r}
plot(reg.summary$cp ,xlab =" Número de Variables ",ylab="Cp",
type='l')
which.min (reg.summary$cp )
points (3, reg.summary$cp [3], col ="red",cex =2, pch =20)
which.min (reg.summary$bic )
plot(reg.summary$bic ,xlab=" Número de Variables ",ylab=" BIC",
type='l')
points (3, reg.summary$bic [3], col =" red",cex =2, pch =20)
```


```{r}
plot(regfit.full ,scale ="r2")
plot(regfit.full ,scale ="adjr2")
plot(regfit.full ,scale ="Cp")
plot(regfit.full ,scale ="bic")
```

```{r}
coef(regfit.full ,3)
```


#### Selección de variables para accidentes graves
```{r}
library (leaps)
regfit.full=regsubsets (ACCIDENTES_LEVES∼Freq_lunes+Freq_martes+Freq_miercoles+Freq_jueves+Freq_viernes+Freq_sabado+Freq_domingo+Feriados+Feriados_Lunes+Feriados_Otro,Train_M_Dataset, nvmax =19)
summary (regfit.full)
```

```{r}
reg.summary =summary(regfit.full)
names(reg.summary)
```


```{r}
reg.summary$rsq
```

```{r}
par(mfrow =c(2,2))
plot(reg.summary$rss ,xlab=" Número de Variables ",ylab=" RSS",
type="l")
plot(reg.summary$adjr2 ,xlab =" Número de Variables ",
ylab=" RSq Ajustado",type="l")
which.max (reg.summary$adjr2)
points (20, reg.summary$adjr2[20], col ="red",cex =2, pch =20)
```

```{r}
plot(reg.summary$cp ,xlab =" Número de Variables ",ylab="Cp",
type='l')
which.min (reg.summary$cp )
points (3, reg.summary$cp [3], col ="red",cex =2, pch =20)
which.min (reg.summary$bic )
plot(reg.summary$bic ,xlab=" Número de Variable ",ylab=" BIC",
type='l')
points (3, reg.summary$bic [3], col =" red",cex =2, pch =20)
```


```{r}
plot(regfit.full ,scale ="r2")
plot(regfit.full ,scale ="adjr2")
plot(regfit.full ,scale ="Cp")
plot(regfit.full ,scale ="bic")
```

```{r}
coef(regfit.full ,3)
```


## 4. Modelamiento

### 1. REGRESION LINEAL

#### Total Accidentes
```{r}
head(Train_M_Dataset)
```



```{r}
library(caret)
trcntrl = trainControl(method="cv", number=10)
caret_lm_fit_m = caret::train(TOTAL_ACCIDENTES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset,
                      method = "lm", trControl = trcntrl,
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```


```{r}
summary(caret_lm_fit_m)
```

```{r}
caret_lm_fit_m
```

Calculo MSE y RMSE para los datos de entrenamiento

```{r}
y_tr_pred_lm_m<-predict(caret_lm_fit_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])

```

```{r}
mse_tr_lm_m<-mean((Train_M_Dataset$TOTAL_ACCIDENTES-y_tr_pred_lm_m)^2) # calcula el mse de entrenamiento
RMSE_tr_lm_m = sqrt(mse_tr_lm_m)
mse_tr_lm_m
RMSE_tr_lm_m
```

Calculo MSE y RMSE para los datos de validación

```{r}
y_test_pred_lm_m<-predict(caret_lm_fit_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_lm_m<-mean((Test_M_Dataset$TOTAL_ACCIDENTES-y_test_pred_lm_m)^2) # calcula el mse de entrenamiento
RMSE_test_lm_m = sqrt(mse_test_lm_m)
mse_test_lm_m
RMSE_test_lm_m
```

Predicción en la muestra

```{r}
library(plotly)
plot_ly (data=Train_M_Dataset,
         x = ~SEMANA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         split = ~ANO,
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_lm_m,
            name='Modelo lm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes',
         xaxis=list(title="Año-Mes"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```


Gráfica serie 2018

```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_lm_m,
            name='Modelo lm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes',
         xaxis=list(title="Año-Mes"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```

#### Accidentes Graves

```{r}
trcntrl = trainControl(method="cv", number=10)
caret_lm_fit_m_m = caret::train(ACCIDENTES_GRAVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset,
                      method = "lm", trControl = trcntrl,
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```


```{r}
summary(caret_lm_fit_m_m)
```

```{r}
caret_lm_fit_m_m
```

Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_lm_m_m<-predict(caret_lm_fit_m_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])

```

```{r}
mse_tr_lm_m_m<-mean((Train_M_Dataset$ACCIDENTES_GRAVES-y_tr_pred_lm_m_m)^2) # calcula el mse de entrenamiento
RMSE_tr_lm_m_m = sqrt(mse_tr_lm_m_m)
mse_tr_lm_m_m
RMSE_tr_lm_m_m
```


Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_lm_m_m<-predict(caret_lm_fit_m_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_lm_m_m<-mean((Test_M_Dataset$ACCIDENTES_GRAVES-y_test_pred_lm_m_m)^2) # calcula el mse de entrenamiento
RMSE_test_lm_m_m = sqrt(mse_test_lm_m_m)
mse_test_lm_m_m
RMSE_test_lm_m_m
```

Predicción en la muestra 

```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_lm_m_m,
            name='Modelo lm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes graves',
         xaxis=list(title="Año-Mes"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))
```


Gráfica serie 2018 

```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_lm_m_m,
            name='Modelo lm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes graves',
         xaxis=list(title="Año-Mes"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))
```



#### Accidentes Leves

```{r}
trcntrl = trainControl(method="cv", number=10)
caret_lm_fit_m_sd = caret::train(ACCIDENTES_LEVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset,
                      method = "lm", trControl = trcntrl,
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```


```{r}
summary(caret_lm_fit_m_sd)
```

```{r}
caret_lm_fit_m_sd
```

Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_lm_m_sd<-predict(caret_lm_fit_m_sd,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])

```

```{r}
mse_tr_lm_m_sd<-mean((Train_M_Dataset$ACCIDENTES_LEVES-y_tr_pred_lm_m_sd)^2) # calcula el mse de entrenamiento
RMSE_tr_lm_m_sd = sqrt(mse_tr_lm_m_sd)
mse_tr_lm_m_sd
RMSE_tr_lm_m_sd
```


Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_lm_m_sd<-predict(caret_lm_fit_m_sd,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_lm_m_sd<-mean((Test_M_Dataset$ACCIDENTES_LEVES-y_test_pred_lm_m_sd)^2) # calcula el mse de entrenamiento
RMSE_test_lm_m_sd = sqrt(mse_test_lm_m_sd)
mse_test_lm_m_sd
RMSE_test_lm_m_sd
```

Predicción en la muestra 

```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_lm_m_sd,
            name='Modelo lm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes leves',
         xaxis=list(title="Año-Semana"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))
```


Gráfica serie 2018 

```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_lm_m_sd,
            name='Modelo lm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes leves',
         xaxis=list(title="Año-Semana"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))
```

#### Resumen Modelos Regresión lineal para los diferentes tipos de accidente

```{r}
Tipo_de_accidentes= c("Total Accidentes","Accidentes graves","Accidentes leves")
RMSE_Train_lm = round(c(RMSE_tr_lm_m,RMSE_tr_lm_m_m,RMSE_tr_lm_m_sd), 3)
RMSE_Test_lm = round(c(RMSE_test_lm_m,RMSE_test_lm_m_m,RMSE_test_lm_m_sd),3)

Tabla_lm = data.frame (cbind(Tipo_de_accidentes,RMSE_Train_lm,RMSE_Test_lm))
Tabla_lm
```



### 2. KNN


```{r}
set.seed(123) # fija la semilla del generador de parámetros para que sea reproducible
```


```{r}
head(Train_M_Dataset)
```

#### Total Accidentes

```{r}
library(caret)
```

```{r}
trcntrl = trainControl(method="cv", number=10)
caret_knn_fit_m = caret::train(TOTAL_ACCIDENTES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset,
                      method = "knn", trControl = trcntrl,
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```

```{r}
summary(caret_knn_fit_m)
```

```{r}
caret_knn_fit_m
```

Calculo MSE y RMSE para los datos de entrenamiento

```{r}
y_tr_pred_knn_m<-predict(caret_knn_fit_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_knn_m<-mean((Train_M_Dataset$TOTAL_ACCIDENTES-y_tr_pred_knn_m)^2) # calcula el mse de entrenamiento
RMSE_tr_knn_m = sqrt(mse_tr_knn_m)
mse_tr_knn_m
RMSE_tr_knn_m
```

Calculo MSE y RMSE para los datos de validación 

```{r}
y_test_pred_knn_m<-predict(caret_knn_fit_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_knn_m<-mean((Test_M_Dataset$TOTAL_ACCIDENTES-y_test_pred_knn_m)^2) # calcula el mse de entrenamiento
RMSE_test_knn_m = sqrt(mse_test_knn_m)
mse_test_knn_m
RMSE_test_knn_m
```
Predicción en la muestra 

```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_knn_m,
            name='Modelo knn',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```
Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_knn_m,
            name='Modelo knn',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```

#### Accidentes graves

```{r}
trcntrl = trainControl(method="cv", number=10)
caret_knn_fit_m_m = caret::train(ACCIDENTES_GRAVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset,
                      method = "knn", trControl = trcntrl,
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```


```{r}
summary(caret_knn_fit_m_m)
```

```{r}
caret_knn_fit_m_m
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_knn_m_m<-predict(caret_knn_fit_m_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_knn_m_m<-mean((Train_M_Dataset$ACCIDENTES_GRAVES-y_tr_pred_knn_m_m)^2) # calcula el mse de entrenamiento
RMSE_tr_knn_m_m = sqrt(mse_tr_knn_m_m)
mse_tr_knn_m_m
RMSE_tr_knn_m_m
```
Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_knn_m_m<-predict(caret_knn_fit_m_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_knn_m_m<-mean((Test_M_Dataset$ACCIDENTES_GRAVES-y_test_pred_knn_m_m)^2) # calcula el mse de entrenamiento
RMSE_test_knn_m_m = sqrt(mse_test_knn_m_m)
mse_test_knn_m_m
RMSE_test_knn_m_m
```
Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_knn_m_m,
            name='Modelo knn',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))
```
Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_knn_m_m,
            name='Modelo knn',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))
```


#### Accidentes Leves


```{r}
trcntrl = trainControl(method="cv", number=10)
caret_knn_fit_m_sd = caret::train(ACCIDENTES_LEVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset,
                      method = "knn", trControl = trcntrl,
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```


```{r}
summary(caret_knn_fit_m_sd)
```

```{r}
caret_knn_fit_m_sd
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_knn_m_sd<-predict(caret_knn_fit_m_sd,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_knn_m_sd<-mean((Train_M_Dataset$ACCIDENTES_LEVES-y_tr_pred_knn_m_sd)^2) # calcula el mse de entrenamiento
RMSE_tr_knn_m_sd = sqrt(mse_tr_knn_m_sd)
mse_tr_knn_m_sd
RMSE_tr_knn_m_sd
```
Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_knn_m_sd<-predict(caret_knn_fit_m_sd,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_knn_m_sd<-mean((Test_M_Dataset$ACCIDENTES_LEVES-y_test_pred_knn_m_sd)^2) # calcula el mse de entrenamiento
RMSE_test_knn_m_sd = sqrt(mse_test_knn_m_sd)
mse_test_knn_m_sd
RMSE_test_knn_m_sd
```
Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_knn_m_sd,
            name='Modelo knn',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```


Gráfica serie 2018 

```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_knn_m_sd,
            name='Modelo knn',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```


#### Resumen Modelos KNN para los diferentes tipos de accidente

```{r}
Tipo_de_accidentes= c("Total Accidentes","Accidentes graves","Accidentes leves")
RMSE_Train_knn = round(c(RMSE_tr_knn_m,RMSE_tr_knn_m_m,RMSE_tr_knn_m_sd), 3)
RMSE_Test_knn = round(c(RMSE_test_knn_m,RMSE_test_knn_m_m,RMSE_test_knn_m_sd),3)

Tabla_knn = data.frame (cbind(Tipo_de_accidentes,RMSE_Train_knn,RMSE_Test_knn))
Tabla_knn
```


### 3. MODELO LINEAL GENERALIZADO


#### Total Accidentes

```{r}
glm_fit_m<-glm(TOTAL_ACCIDENTES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset, family = "poisson")
summary(glm_fit_m)
```

```{r}
glm_fit_m
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_glm_m<-predict(glm_fit_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_glm_m<-mean((Train_M_Dataset$TOTAL_ACCIDENTES-y_tr_pred_glm_m)^2) # calcula el mse de entrenamiento
RMSE_tr_glm_m = sqrt(mse_tr_glm_m)
mse_tr_glm_m
RMSE_tr_glm_m
```

Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_glm_m<-predict(glm_fit_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_glm_m<-mean((Train_M_Dataset$TOTAL_ACCIDENTES-y_test_pred_glm_m)^2) # calcula el mse de entrenamiento
```
```{r}
RMSE_test_glm_m = sqrt(mse_test_glm_m)
mse_test_glm_m
RMSE_test_glm_m
```
Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_glm_m,
            name='Modelo glm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```

Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_glm_m,
            name='Modelo glm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```


#### Accidentes Graves

```{r}
glm_fit_m_m<-glm(ACCIDENTES_GRAVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset, family = "poisson")
summary(glm_fit_m)
```

```{r}
glm_fit_m_m
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_glm_m_m<-predict(glm_fit_m_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_glm_m_m<-mean((Train_M_Dataset$ACCIDENTES_GRAVES-y_tr_pred_glm_m_m)^2) # calcula el mse de entrenamiento
RMSE_tr_glm_m_m = sqrt(mse_tr_glm_m_m)
mse_tr_glm_m_m
RMSE_tr_glm_m_m
```

Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_glm_m_m<-predict(glm_fit_m_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_glm_m_m<-mean((Train_M_Dataset$ACCIDENTES_GRAVES-y_test_pred_glm_m_m)^2) # calcula el mse de entrenamiento
```
```{r}
RMSE_test_glm_m_m = sqrt(mse_test_glm_m_m)
mse_test_glm_m_m
RMSE_test_glm_m_m
```
Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_glm_m_m,
            name='Modelo glm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))
```

Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_glm_m_m,
            name='Modelo glm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))
```



#### Accidentes Leves

```{r}
glm_fit_m_sd<-glm(ACCIDENTES_LEVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset, family = "poisson")
summary(glm_fit_m_sd)
```

```{r}
glm_fit_m_sd
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_glm_m_sd<-predict(glm_fit_m_sd,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_glm_m_sd<-mean((Train_M_Dataset$ACCIDENTES_LEVES-y_tr_pred_glm_m_sd)^2) # calcula el mse de entrenamiento
RMSE_tr_glm_m_sd = sqrt(mse_tr_glm_m_sd)
mse_tr_glm_m_sd
RMSE_tr_glm_m_sd
```

Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_glm_m_sd<-predict(glm_fit_m_sd,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_glm_m_sd<-mean((Train_M_Dataset$ACCIDENTES_LEVES-y_test_pred_glm_m_sd)^2) # calcula el mse de entrenamiento
```
```{r}
RMSE_test_glm_m_sd = sqrt(mse_test_glm_m_sd)
mse_test_glm_m_sd
RMSE_test_glm_m_sd
```
Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_glm_m_sd,
            name='Modelo glm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))
```

Gráfica serie 2018  
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_glm_m_sd,
            name='Modelo glm',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total Accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))
```
#### Resumen Modelos Regresión lineal generalizado para los diferentes tipos de accidente

```{r}
Tipo_de_accidentes= c("Total Accidentes","Accidentes Graves","Accidentes Leves")
RMSE_Train_glm = round(c(RMSE_tr_glm_m,RMSE_tr_glm_m_m,RMSE_tr_glm_m_sd), 3)
RMSE_Test_glm = round(c(RMSE_test_glm_m,RMSE_test_glm_m_m,RMSE_test_glm_m_sd),3)

Tabla_glm = data.frame (cbind(Tipo_de_accidentes,RMSE_Train_glm,RMSE_Test_glm))
Tabla_glm
```




### 4. ARBOLES DE REGRESION

#### Total Accidentes

```{r}
trcntrl = trainControl(method="cv", number=10)
caret_tree_fit_m = caret::train(TOTAL_ACCIDENTES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros,data=Train_M_Dataset,
                              method = "rpart", trControl = trcntrl,
                      parms = list(split = "gini"),
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```



```{r}
caret_tree_fit_m
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_tree_m<-predict(caret_tree_fit_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_tree_m<-mean((Train_M_Dataset$TOTAL_ACCIDENTES-y_tr_pred_tree_m)^2) # calcula el mse de entrenamiento
RMSE_tr_tree_m = sqrt(mse_tr_tree_m)
mse_tr_tree_m
RMSE_tr_tree_m
```
Calculo MSE y RMSE para los datos de validación 
```{r}
  y_test_pred_tree_m<-predict(caret_tree_fit_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_tree_m<-mean((Test_M_Dataset$TOTAL_ACCIDENTES-y_test_pred_tree_m)^2) # calcula el mse de entrenamiento
RMSE_test_tree_m = sqrt(mse_test_tree_m)
mse_test_tree_m
RMSE_test_tree_m
```
Predicción en la muestra  
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_tree_m,
            name='Modelo tree',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```
Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_tree_m,
            name='Modelo tree',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```


#### Accidentes graves

```{r}
trcntrl = trainControl(method="cv", number=10)
caret_tree_fit_m_m = caret::train(ACCIDENTES_GRAVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros,data=Train_M_Dataset,
                              method = "rpart", trControl = trcntrl,
                      parms = list(split = "gini"),
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```

```{r}
caret_tree_fit_m_m
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_tree_m_m<-predict(caret_tree_fit_m_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_tree_m_m<-mean((Train_M_Dataset$ACCIDENTES_GRAVES-y_tr_pred_tree_m_m)^2) # calcula el mse de entrenamiento
RMSE_tr_tree_m_m = sqrt(mse_tr_tree_m_m)
mse_tr_tree_m_m
RMSE_tr_tree_m_m
```
Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_tree_m_m<-predict(caret_tree_fit_m_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_tree_m_m<-mean((Test_M_Dataset$ACCIDENTES_GRAVES-y_test_pred_tree_m_m)^2) # calcula el mse de entrenamiento
RMSE_test_tree_m_m = sqrt(mse_test_tree_m_m)
mse_test_tree_m_m
RMSE_test_tree_m_m
```
Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_tree_m_m,
            name='Modelo tree',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```
Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_tree_m_m,
            name='Modelo tree',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```



#### Accidentes leves

```{r}
trcntrl = trainControl(method="cv", number=10)
caret_tree_fit_m_sd = caret::train(ACCIDENTES_LEVES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros,data=Train_M_Dataset,
                              method = "rpart", trControl = trcntrl,
                      parms = list(split = "gini"),
                      preProcess=c("center", "scale"),
                      tuneLength = 10)
```

```{r}
caret_tree_fit_m_sd
```
Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_tree_m_sd<-predict(caret_tree_fit_m_sd,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_tree_m_sd<-mean((Train_M_Dataset$ACCIDENTES_LEVES-y_tr_pred_tree_m_sd)^2) # calcula el mse de entrenamiento
RMSE_tr_tree_m_sd = sqrt(mse_tr_tree_m_sd)
mse_tr_tree_m_sd
RMSE_tr_tree_m_sd
```

Calculo MSE y RMSE para los datos de validación 
```{r}
  y_test_pred_tree_m_sd<-predict(caret_tree_fit_m_sd,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_tree_m_sd<-mean((Test_M_Dataset$ACCIDENTES_LEVES-y_test_pred_tree_m_sd)^2) # calcula el mse de entrenamiento
RMSE_test_tree_m_sd = sqrt(mse_test_tree_m_sd)
mse_test_tree_m_sd
RMSE_test_tree_m_sd
```

Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_tree_m_sd,
            name='Modelo tree',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))
```
Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_tree_m_sd,
            name='Modelo tree',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))
```

#### Resumen Modelos Árboles de regresión para los diferentes tipos de accidente

```{r}
Tipo_de_accidentes= c("Total Accidentes","Accidentes Graves","Accidentes Leves")
RMSE_Train_tree = round(c(RMSE_tr_tree_m,RMSE_tr_tree_m_m,RMSE_tr_tree_m_sd), 3)
RMSE_Test_tree = round(c(RMSE_test_tree_m,RMSE_test_tree_m_m,RMSE_test_tree_m_sd),3)

Tabla_tree = data.frame (cbind(Tipo_de_accidentes,RMSE_Train_tree,RMSE_Test_tree))
Tabla_tree
```

### 5. BOSQUE ALEATORIO


#### Total Accidentes

```{r Random_Forest_TOTAL_ACCIDENTES}
trcntrl = trainControl(method="cv", number=10)
caret_rf_fit_m = caret::train(TOTAL_ACCIDENTES∼Feria_Flores_Semana+Feriados_Lunes+Feriados_Otros, data=Train_M_Dataset,
                      method = "rf", trControl = trcntrl,
                      prox=TRUE,allowParallel=TRUE)
```


```{r}
summary(caret_rf_fit_m)
```

```{r}
caret_rf_fit_m
```

```{r}
plot(caret_rf_fit_m)
```

Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_rf_m<-predict(caret_rf_fit_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_rf_m<-mean((Train_M_Dataset$TOTAL_ACCIDENTES-y_tr_pred_rf_m)^2) # calcula el mse de entrenamiento
RMSE_tr_rf_m = sqrt(mse_tr_rf_m)
mse_tr_rf_m
RMSE_tr_rf_m
```

Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_rf_m<-predict(caret_rf_fit_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_rf_m<-mean((Test_M_Dataset$TOTAL_ACCIDENTES-y_test_pred_rf_m)^2) # calcula el mse de entrenamiento
RMSE_test_rf_m = sqrt(mse_test_rf_m)
mse_test_rf_m
RMSE_test_rf_m
```


Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_rf_m,
            name='Modelo rf',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))
```


Gráfica serie 2018 

```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~TOTAL_ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y=  ~y_test_pred_rf_m,
            name='Modelo rf',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 0.75, y = 0.9))

```


#### Accidentes Graves

```{r Random_Forest_ACCIDENTES_GRAVES}
trcntrl = trainControl(method="cv", number=10)
caret_rf_fit_m_m = caret::train(ACCIDENTES_GRAVES~Ano_v1+Ano_v1*DIA_SEMANA+SEMANA+DIA_SEMANA+Semana.Santa+Feriado_Lunes+Feriado_Otro+Feria_Flores+Madre, data=Train_M_Dataset,
                      method = "rf", trControl = trcntrl,
                      prox=TRUE,allowParallel=TRUE)
```


```{r}
summary(caret_rf_fit_m_m)
```

```{r}
caret_rf_fit_m_m
```

```{r}
plot(caret_rf_fit_m_m)
```

Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_rf_m_m<-predict(caret_rf_fit_m_m,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_rf_m_m<-mean((Train_M_Dataset$ACCIDENTES_GRAVES-y_tr_pred_rf_m_m)^2) # calcula el mse de entrenamiento
RMSE_tr_rf_m_m = sqrt(mse_tr_rf_m_m)
mse_tr_rf_m_m
RMSE_tr_rf_m_m
```

Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_rf_m_m<-predict(caret_rf_fit_m_m,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_rf_m_m<-mean((Test_M_Dataset$ACCIDENTES_GRAVES-y_test_pred_rf_m_m)^2) # calcula el mse de entrenamiento
RMSE_test_rf_m_m = sqrt(mse_test_rf_m_m)
mse_test_rf_m_m
RMSE_test_rf_m_m
```

Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_rf_m_m,
            name='Modelo rf',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))
```

Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_GRAVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_rf_m_m,
            name='Modelo rf',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes graves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes graves"),
         legend = list(x = 0.75, y = 0.9))

```

#### Accidentes Leves

```{r Random_Forest_ACCIDENTES_LEVES}
trcntrl = trainControl(method="cv", number=10)
caret_rf_fit_m_sd = caret::train(ACCIDENTES_LEVES~Ano_v1+Ano_v1*DIA_SEMANA+SEMANA+DIA_SEMANA+Semana.Santa+Feriado_Lunes+Feriado_Otro+Feria_Flores+Madre, data=Train_M_Dataset,
                      method = "rf", trControl = trcntrl,
                      prox=TRUE,allowParallel=TRUE)
```


```{r}
summary(caret_rf_fit_m_sd)
```

```{r}
caret_rf_fit_m_sd
```

```{r}
plot(caret_rf_fit_m_sd)
```

Calculo MSE y RMSE para los datos de entrenamiento 
```{r}
y_tr_pred_rf_m_sd<-predict(caret_rf_fit_m_sd,Train_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_tr_rf_m_sd<-mean((Train_M_Dataset$ACCIDENTES_LEVES-y_tr_pred_rf_m_sd)^2) # calcula el mse de entrenamiento
RMSE_tr_rf_m_sd = sqrt(mse_tr_rf_m_sd)
mse_tr_rf_m_sd
RMSE_tr_rf_m_sd
```

Calculo MSE y RMSE para los datos de validación 
```{r}
y_test_pred_rf_m_md<-predict(caret_rf_fit_m_sd,Test_M_Dataset[,c("Feria_Flores_Semana","Feriados_Lunes","Feriados_Otros")])
mse_test_rf_m_sd<-mean((Test_M_Dataset$ACCIDENTES_LEVES-y_test_pred_rf_m_md)^2) # calcula el mse de entrenamiento
RMSE_test_rf_m_sd = sqrt(mse_test_rf_m_sd)
mse_test_rf_m_sd
RMSE_test_rf_m_sd
```


Predicción en la muestra 
```{r}
plot_ly (data=Train_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_tr_pred_rf_m_sd,
            name='Modelo rf',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))
```

Gráfica serie 2018 
```{r}
plot_ly (data=Test_M_Dataset,
         x = ~FECHA,
         y = ~ACCIDENTES_LEVES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~y_test_pred_rf_m_md,
            name='Modelo rf',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  layout(title='Total accidentes leves',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes leves"),
         legend = list(x = 0.75, y = 0.9))

```


#### Resumen Modelos Random Forest para los diferentes tipos de accidente

```{r}
Tipo_de_accidentes= c("Total Accidentes","Total Graves","Total Leves")
RMSE_Train_rf = round(c(RMSE_tr_rf_m,RMSE_tr_rf_m_m,RMSE_tr_rf_m_sd), 3)
RMSE_Test_rf = round(c(RMSE_test_rf_m,RMSE_test_rf_m_m,RMSE_test_rf_m_sd),3)

Tabla_rf = data.frame (cbind(Tipo_de_accidentes,RMSE_Train_rf,RMSE_Test_rf))
Tabla_rf
```



## ELECCION DEL MODELO


### 1. Elección del modelo para el total de accidentes

Comparación en el entrenamiento
```{r}
comparacion_tr<-data.frame(FECHA=Total_Dataset_Freq_M$FECHA[Total_Dataset_Freq_M$FECHA<="2017-12-31"],
                          ACCIDENTES=Total_Dataset_Freq_M$TOTAL_ACCIDENTES[Total_Dataset_Freq_M$FECHA<="2017-12-31"],
                           lm= y_tr_pred_lm_m, 
                           knn= y_tr_pred_knn_m, 
                           glm=y_tr_pred_glm_m ,
                           arbol=y_tr_pred_tree_m,
                           rf=y_tr_pred_rf_m)
```

```{r}
plot_ly (data=comparacion_tr,
         x = ~FECHA,
         y = ~ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~lm,
            name='lm',
            line=list(width=1,color= "blue"))%>%
  add_trace(y= ~knn,
            name='knn',
            line=list(width=1,color="red"))%>%
  add_trace(y= ~glm,
            name='Modelo Poisson',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  add_trace(y= ~arbol,
            name='Árbol',
            line=list(width=1,color="green"))%>%
    add_trace(y= ~rf,
            name='Bosque',
            line=list(width=1,color='rgb(255, 51, 153)'))%>%
  layout(title='Registros RUNT (Entrenamiento)',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Unidades"),
         legend = list(x = 1, y = 0.9))
```


Comparación en la validación
```{r}
comparacion_vl<-data.frame(FECHA=Test_M_Dataset$FECHA,
                          ACCIDENTES=Test_M_Dataset$TOTAL_ACCIDENTES,
                           lm= y_test_pred_lm_m, 
                           knn= y_test_pred_knn_m, 
                           glm=y_test_pred_glm_m ,
                           arbol=y_test_pred_tree_m,
                           rf=y_test_pred_rf_m)
```

```{r}
plot_ly (data=comparacion_vl,
         x = ~FECHA,
         y = ~ACCIDENTES,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~lm,
            name='lm',
            line=list(width=1,color= "blue"))%>%
  add_trace(y= ~knn,
            name='knn',
            line=list(width=1,color="red"))%>%
  add_trace(y= ~glm,
            name='Modelo Poisson',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  add_trace(y= ~arbol,
            name='Árbol',
            line=list(width=1,color="green"))%>%
  add_trace(y= ~rf,
            name='Bosque',
            line=list(width=1,color='rgb(255, 51, 153)'))%>%
  layout(title='Total Accidentes (Validación)',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 1, y = 0.9))
```


#### Comparación con los RMSE:

```{r}
Entrenamiento<-round(c(Rmse_tr_lm_m,RMSE_tr_knn_m,RMSE_tr_glm_m,RMSE_tr_tree_m,RMSE_tr_rf_m),3) 
Validacion<-round(c(RMSE_test_lm_m,RMSE_test_knn_m,RMSE_test_glm_m,RMSE_test_tree_m,RMSE_test_rf_m),3) 
nombres<-c("lm","knn","glm","árbol","bosque")
ResultadosRMSE<-data.frame(Entrenamiento=Entrenamiento,Validacion=Validacion)
rownames(ResultadosRMSE)<-nombres
ResultadosRMSE
```


### 2. Elección del modelo para Accidentes graves

Comparación en el entrenamiento
```{r}
comparacion_tr<-data.frame(FECHA=Total_Dataset_Freq_M$FECHA[Total_Dataset_Freq_M$FECHA<="2017-12-31"],
                          ACCIDENTESG=Total_Dataset_Freq_M$ACCIDENTES_GRAVES[Total_Dataset_Freq_M$FECHA<="2017-12-31"],
                           lm= y_tr_pred_lm_m_m, 
                           knn= y_tr_pred_knn_m_m, 
                           glm=y_tr_pred_glm_m_m,
                           arbol=y_tr_pred_tree_m_m,
                           rf=y_tr_pred_rf_m_m)
```

```{r}
plot_ly (data=comparacion_tr,
         x = ~FECHA,
         y = ~ACCIDENTESG,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~lm,
            name='lm-graves',
            line=list(width=1,color= "blue"))%>%
  add_trace(y= ~knn,
            name='knn-graves',
            line=list(width=1,color="red"))%>%
  add_trace(y= ~glm,
            name='Modelo Poisson-graves',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  add_trace(y= ~arbol,
            name='Árbo-gravesl',
            line=list(width=1,color="green"))%>%
    add_trace(y= ~rf,
            name='Bosque-graves',
            line=list(width=1,color='rgb(255, 51, 153)'))%>%
  layout(title='Accidentes graves (Entrenamiento)',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 1, y = 0.9))
```


Comparación en la validación
```{r}
comparacion_vl<-data.frame(FECHA=Test_M_Dataset$FECHA,
                          ACCIDENTESG=Test_M_Dataset$ACCIDENTES_GRAVES,
                           lm= y_test_pred_lm_m_m, 
                           knn= y_test_pred_knn_m_m, 
                           glm=y_test_pred_glm_m_m,
                           arbol=y_test_pred_tree_m_m,
                           rf=y_test_pred_rf_m_m)
```

```{r}
plot_ly (data=comparacion_vl,
         x = ~FECHA,
         y = ~ACCIDENTESG,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~lm,
            name='lm-graves',
            line=list(width=1,color= "blue"))%>%
  add_trace(y= ~knn,
            name='knn-graves',
            line=list(width=1,color="red"))%>%
  add_trace(y= ~glm,
            name='Modelo Poisson-graves',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  add_trace(y= ~arbol,
            name='Árbol-graves',
            line=list(width=1,color="green"))%>%
  add_trace(y= ~rf,
            name='Bosque-graves',
            line=list(width=1,color='rgb(255, 51, 153)'))%>%
  layout(title='Accidentes graves (Validación)',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 1, y = 0.9))
```



#### Comparación con los RMSE:

```{r}
Entrenamiento<-round(c(Rmse_tr_lm_m_m,RMSE_tr_knn_m_m,RMSE_tr_glm_m_m,RMSE_tr_tree_m_m,RMSE_tr_rf_m_m),3) 
Validacion<-round(c(RMSE_test_lm_m_m,RMSE_test_knn_m_m,RMSE_test_glm_m_m,RMSE_test_tree_m_m,RMSE_test_rf_m_m),3) 
nombres<-c("lm","knn","glm","árbol","bosque")
ResultadosRMSE<-data.frame(Entrenamiento=Entrenamiento,Validacion=Validacion)
rownames(ResultadosRMSE)<-nombres
ResultadosRMSE
```


### 3. Elección del modelo para Accidentes leves

Comparación en el entrenamiento
```{r}
comparacion_tr<-data.frame(FECHA=Total_Dataset_Freq_M$FECHA[Total_Dataset_Freq_M$FECHA<="2017-12-31"],
                          ACCIDENTESL=Total_Dataset_Freq_M$ACCIDENTES_LEVES[Total_Dataset_Freq_M$FECHA<="2017-12-31"],
                           lm= y_tr_pred_lm_m_sd, 
                           knn= y_tr_pred_knn_m_sd, 
                           glm=y_tr_pred_glm_m_sd,
                           arbol=y_tr_pred_tree_m_sd,
                           rf=y_tr_pred_rf_m_sd)
```

```{r}
plot_ly (data=comparacion_tr,
         x = ~FECHA,
         y = ~ACCIDENTESL,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~lm,
            name='lm-lesves',
            line=list(width=1,color= "blue"))%>%
  add_trace(y= ~knn,
            name='knn-leves',
            line=list(width=1,color="red"))%>%
  add_trace(y= ~glm-graves,
            name='Modelo Poisson-leves',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  add_trace(y= ~arbol,
            name='Árbo-gravesl',
            line=list(width=1,color="green"))%>%
    add_trace(y= ~rf,
            name='Bosque-graves',
            line=list(width=1,color='rgb(255, 51, 153)'))%>%
  layout(title='Accidentes graves (Entrenamiento)',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 1, y = 0.9))
```


Comparación en la validación
```{r}
comparacion_vl<-data.frame(FECHA=Test_M_Dataset$FECHA,
                          ACCIDENTESL=Test_M_Dataset$ACCIDENTES_GRAVES,
                           lm= y_test_pred_lm_m_sd, 
                           knn= y_test_pred_knn_m_sd, 
                           glm=y_test_pred_glm_m_sd,
                           arbol=y_test_pred_tree_m_sd,
                           rf=y_test_pred_rf_m_md)
```

```{r}
plot_ly (data=comparacion_vl,
         x = ~FECHA,
         y = ~ACCIDENTESL,
         type = "scatter" ,mode = "lines",
         name='Real',
         line=list(width=1,color='rgb(205, 12, 24)'))%>%
  add_trace(y= ~lm,
            name='lm-leves',
            line=list(width=1,color= "blue"))%>%
  add_trace(y= ~knn,
            name='knn',
            line=list(width=1,color="red"))%>%
  add_trace(y= ~glm,
            name='Modelo Poisson-leves',
            line=list(width=1,color='rgb(22, 96, 167)'))%>%
  add_trace(y= ~arbol,
            name='Árbol-leves',
            line=list(width=1,color="green"))%>%
  add_trace(y= ~rf,
            name='Bosque-leves',
            line=list(width=1,color='rgb(255, 51, 153)'))%>%
  layout(title='Accidentes lesves (Validación)',
         xaxis=list(title="Fecha"),
         yaxis=list(title="Accidentes"),
         legend = list(x = 1, y = 0.9))
```



#### Comparación con los RMSE:

```{r}
Entrenamiento<-round(c(Rmse_tr_lm_m_sd,RMSE_tr_knn_m_sd,RMSE_tr_glm_m_sd,RMSE_tr_tree_m_sd,RMSE_tr_rf_m_sd),3) 
Validacion<-round(c(RMSE_test_lm_m_sd,RMSE_test_knn_m_sd,RMSE_test_glm_m_sd,RMSE_test_tree_m_sd,RMSE_test_rf_m_sd),3) 
nombres<-c("lm","knn","glm","árbol","bosque")
ResultadosRMSE<-data.frame(Entrenamiento=Entrenamiento,Validacion=Validacion)
rownames(ResultadosRMSE)<-nombres
ResultadosRMSE
```
